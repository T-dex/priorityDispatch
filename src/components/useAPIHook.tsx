import { useEffect, useState } from 'react';

const useAPIHook = () => {
  const [state, setstate] = useState();

    useEffect(() => {
      fetch({CUSTOMAPI})
     return;
  }, []);

  return (
     <>
       
     </>
   );
 };

export default useAPIHook;